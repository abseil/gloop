name: Publish to BCR

on:
  release:
    types: [published]
  workflow_call:
    inputs:
      tag_name:
        description: 'Release tag to publish to BCR (e.g. 20260826.rc1 or 20260826.0)'
        required: false
        type: string
      registry_fork:
        description: 'Fork of bazel-central-registry to push to (default: abseil/bazel-central-registry)'
        required: false
        default: 'abseil/bazel-central-registry'
        type: string
    secrets:
      publish_token:
        description: 'GitHub token or personal access token with permissions to push to registry fork'
        required: false
  workflow_dispatch:
    inputs:
      tag_name:
        description: 'Release tag to publish to BCR (e.g. 20260826.rc1 or 20260826.0)'
        required: true
        type: string
      registry_fork:
        description: 'Fork of bazel-central-registry to push to (default: abseil/bazel-central-registry)'
        required: false
        default: 'abseil/bazel-central-registry'
        type: string

jobs:
  bcr:
    name: Publish Module to Bazel Central Registry
    permissions:
      contents: write
      id-token: write
      attestations: write
    uses: bazel-contrib/publish-to-bcr/.github/workflows/publish.yaml@ad6879fd015a01c3e3b1b318fe3c061f928ac2fb # v1.4.2
    with:
      tag_name: ${{ inputs.tag_name || github.event.release.tag_name }}
      registry_fork: ${{ inputs.registry_fork || 'abseil/bazel-central-registry' }}
      attest: false
    secrets:
      publish_token: ${{ secrets.publish_token || secrets.BCR_PUBLISH_TOKEN }}
